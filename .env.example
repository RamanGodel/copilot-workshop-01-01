# PostgreSQL Configuration
POSTGRES_DB=currency_exchange
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123

# Spring Profile
SPRING_PROFILES_ACTIVE=dev

# External Providers (Phase 5)
# Fill these in locally in a .env file (do not commit .env)
PROVIDER_FIXER_ACCESS_KEY=
PROVIDER_EXCHANGERATESAPI_ACCESS_KEY=

# Optional overrides
PROVIDER_FIXER_BASE_URL=https://data.fixer.io/api
PROVIDER_EXCHANGERATESAPI_BASE_URL=https://api.exchangeratesapi.io/v1
